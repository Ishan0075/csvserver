#!/bin/bash


file=/test/csvserver/inputfile

echo "$1, $2" > ${file}

echo "$3, $4" >> ${file}

echo "$5, $6" >> ${file}

echo "$7, $8" >> ${file}

echo "$9, ${10}" >> ${file}

echo "${11}, ${12}" >> ${file}

echo "${13}, ${14}" >> ${file}